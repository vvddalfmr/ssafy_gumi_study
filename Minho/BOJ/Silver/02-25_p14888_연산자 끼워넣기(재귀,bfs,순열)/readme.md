
# [BOJ] 14888 - 연산자 끼워넣기 (Java)

## 🔗 문제 링크
[백준 14888번: 연산자 끼워넣기](https://www.acmicpc.net/problem/14888)


---
## 📊 성능 분석 (Performance)

| 메모리 (Memory) | 시간 (Time) | 언어 (Language) | 코드 길이 (Code Length) |
| :---: | :---: | :---: | :---: |
| **16112 KB** | **136 ms** | **Java 11** | **1376 B** |


## 📌 문제 개요
N 개의 수가 주어지고 4 종류의 연산자의 개수가 주어지고 총 합은 N-1입니다. 연산자의 우선순위는 무시하고 앞에서부터 계산을 진행합니다.연산자의 순서를 바꾸어 적절히 배치하였을 떄, 나올 수 있는 결과의 최댓값과 최솟값을 구하세요.


---

## 💡 해결 프로세스
 1. 입력값 N의 최대 크기가 11임을 인지하고 중복 순열로 접근합니다.
 2. 종류 별로 사용할 수있는 연산자의 개수가 정해져 있으므로 사용할 수 있는 연산자의 개수를 저장하는 counting 배열을 만들고 백트래킹을 시도합니다.  
 3. 레벨과 결과를 상태로 지정한 백트래킹 중복 순열을 시도합니다. 

---

## 💻 코드 구조 상세 (Core Logic)

🔍 뒤에서 m-1 개 만큼 n-m 번째 노드에 연결
```Java
    //결과를 상태로 관리하여 탈출 조건에서 바로 비교를 수행합니다. 연산자 개수 배열은 백트래킹 
    static public void DoDFS(int lv,int ret) {
		if(lv >= numOpers) {
			min = Math.min(min, ret);
			max = Math.max(max, ret);
			return ; 
		}
		
		for(int i = 0 ; i< 4;++i) {
			if(opers[i]==0) continue ;
			--opers[i];
			int newRet = ret;
			switch(i) {
			case 0: newRet  += nums[lv+1];break;
			case 1: newRet  -= nums[lv+1];break;
			case 2: newRet  *= nums[lv+1];break;
			case 3: newRet  /= nums[lv+1];break;
			}
			DoDFS(lv+1,newRet);
			++opers[i];
		}
		
	}
```



---
⚠️ 주의 및 회고
 결과값을 재귀의 상태로 전달하면, 편하게 답을 도출할 수 있다.
